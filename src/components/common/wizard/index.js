import { stepState } from './newTypes'
import { FormikWizard } from './FormkWizard'
import { useFormikWizard } from './useFormikWizard'
import { modes } from './modeTypes'
// import { Step, RenderProps, WizardProps } from './type_s'
const { Step, RenderProps, WizardProps } = require('./types')

export { FormikWizard, modes, useFormikWizard, Step, stepState, RenderProps, WizardProps }
